# Motif Lab v0.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 打通「循环」概念：骨架秒出 → 故事动画（缓存/生成）→ 不懂升 L 级 → 口诀 + 界面检验题。

**Architecture:** 独立薄壳应用（非 Tepeu fork）。后端编排母题会话；`StoryboardService` 先出分镜；`DemoCache` + 内置金牌 HTML 保证秒播；未命中则 `AnimationGenerator` 调 LLM 写单文件 HTML。前端左右两栏：对话 + sandbox iframe。

**Tech Stack:** Java 21、Spring Boot 4.0.7、Spring AI 2.0.0、SQLite（仅存 provider 配置）、React 18、Vite 6、TypeScript 5、Tailwind 4。端口 **30142**（避开 Tepeu 30141）。

**Spec:** `docs/superpowers/specs/2026-07-12-motif-lab-design.md`

---

## File structure（锁定）

```
motif-lab/
  CONTEXT.md
  README.md
  .gitignore
  backend/
    pom.xml
    src/main/java/com/motiflab/
      MotifLabApplication.java
      config/DatabaseConfig.java
      config/WebConfig.java
      model/{MotifSession,Storyboard,QuizItem,ProviderConfig}.java
      dto/{StartLessonRequest,SimplifyRequest,RewriteRequest,AnswerRequest,ApiResponse}.java
      service/
        ConceptNormalizer.java
        DemoCache.java
        StoryboardService.java
        AnimationGenerator.java
        MotifTutorService.java
        LlmClient.java
        ProviderService.java
        CryptoService.java          # 可从 Tepeu 精简拷贝
      controller/{LessonController,ProviderController,DemoController}.java
    src/main/resources/
      application.yml
      demos/loop/L0.html
      demos/loop/L1.html
      demos/loop/L2.html
      demos/loop/storyboard.json
      prompts/motif-tutor.md
      prompts/animation-gen.md
      static/                       # frontend build 输出
    src/test/java/com/motiflab/
      service/{ConceptNormalizerTest,DemoCacheTest,StoryboardServiceTest,MotifTutorServiceTest}.java
  frontend/
    package.json
    vite.config.ts
    index.html
    src/
      main.tsx
      App.tsx
      api/client.ts
      types.ts
      components/
        LessonLayout.tsx
        ChatPanel.tsx
        StagePanel.tsx
        QuizCard.tsx
        ProviderSettings.tsx
      styles/index.css
```

运行时动画缓存目录（gitignore）：`backend/data/demo-cache/`、`backend/motif-lab.db*`。

---

### Task 1: 仓库脚手架 + .gitignore

**Files:**
- Create: `E:/work/motif-lab/.gitignore`
- Create: `E:/work/motif-lab/README.md`
- Create: `E:/work/motif-lab/backend/pom.xml`
- Create: `E:/work/motif-lab/backend/src/main/java/com/motiflab/MotifLabApplication.java`
- Create: `E:/work/motif-lab/backend/src/main/resources/application.yml`
- Create: `E:/work/motif-lab/frontend/package.json`
- Create: `E:/work/motif-lab/frontend/vite.config.ts`
- Create: `E:/work/motif-lab/frontend/tsconfig.json`
- Create: `E:/work/motif-lab/frontend/index.html`
- Create: `E:/work/motif-lab/frontend/src/main.tsx`
- Create: `E:/work/motif-lab/frontend/src/App.tsx`
- Create: `E:/work/motif-lab/frontend/src/styles/index.css`

- [ ] **Step 1: 写 `.gitignore`**

```gitignore
backend/target/
backend/*.db
backend/*.db-*
backend/data/
frontend/node_modules/
frontend/dist/
.env
.env.*
*.key
.tepeu/
.idea/
.vscode/
```

- [ ] **Step 2: 写最小 `backend/pom.xml`**

沿用 Tepeu：`spring-boot-starter-parent` **4.0.7**、`java.version` **21**、`spring-ai.version` **2.0.0**；依赖：`spring-boot-starter-web`、`spring-boot-starter-jdbc`、`org.xerial:sqlite-jdbc`、`spring-ai-starter-model-openai`、`spring-ai-starter-model-anthropic`、`spring-boot-starter-test`。`artifactId`：`motif-lab-backend`，`groupId`：`com.motiflab`。

- [ ] **Step 3: 启动类与配置**

`MotifLabApplication.java`：标准 `@SpringBootApplication`。

`application.yml`：

```yaml
server:
  port: 30142
spring:
  application:
    name: motif-lab
  datasource:
    url: jdbc:sqlite:motif-lab.db
    driver-class-name: org.sqlite.JDBC
motiflab:
  demo-cache-dir: ${user.dir}/data/demo-cache
  protocol-version: v1
  security:
    master-key-file: ${user.home}/.motif-lab/master.key
```

- [ ] **Step 4: 前端最小 Vite React**

`package.json` 脚本：`dev` / `build`（`tsc && vite build`）。`vite.config.ts`：`outDir: '../backend/src/main/resources/static'`，proxy `/api` → `http://127.0.0.1:30142`。`App.tsx` 先渲染标题「母题实验室」。

- [ ] **Step 5: 验证脚手架**

```bash
cd E:/work/motif-lab/backend && mvn -q -DskipTests compile
cd E:/work/motif-lab/frontend && npm install && npm run build
```

Expected: compile/build 成功。

- [ ] **Step 6: Commit**

```bash
cd E:/work/motif-lab && git init -b main
git add -A && git commit -m "chore: scaffold motif-lab backend and frontend"
```

---

### Task 2: ConceptNormalizer + 缓存键（TDD）

**Files:**
- Create: `backend/src/main/java/com/motiflab/service/ConceptNormalizer.java`
- Create: `backend/src/test/java/com/motiflab/service/ConceptNormalizerTest.java`

- [ ] **Step 1: 写失败测试**

```java
@Test
void normalize_loopAliases() {
    ConceptNormalizer n = new ConceptNormalizer();
    assertEquals("loop", n.normalize("循环"));
    assertEquals("loop", n.normalize(" for 循环 "));
    assertEquals("loop", n.normalize("LOOP"));
}

@Test
void cacheKey_includesLevelAndProtocol() {
    ConceptNormalizer n = new ConceptNormalizer();
    assertEquals("loop|L0|v1", n.cacheKey("循环", 0, "v1"));
    assertEquals("loop|L2|v1", n.cacheKey("循环", 2, "v1"));
}

@Test
void clampLevel() {
    ConceptNormalizer n = new ConceptNormalizer();
    assertEquals(0, n.clampLevel(-1));
    assertEquals(2, n.clampLevel(99));
}
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd E:/work/motif-lab/backend && mvn -q -Dtest=ConceptNormalizerTest test
```

Expected: FAIL（类不存在）。

- [ ] **Step 3: 实现**

```java
public class ConceptNormalizer {
    public String normalize(String raw) {
        if (raw == null) return "unknown";
        String s = raw.trim().toLowerCase(Locale.ROOT);
        if (s.contains("循环") || s.contains("loop") || s.contains("for") || s.contains("while")) {
            return "loop";
        }
        return s.replaceAll("[^a-z0-9\\u4e00-\\u9fff]+", "-");
    }
    public int clampLevel(int level) {
        return Math.max(0, Math.min(2, level));
    }
    public String cacheKey(String concept, int level, String protocolVersion) {
        return normalize(concept) + "|L" + clampLevel(level) + "|" + protocolVersion;
    }
}
```

- [ ] **Step 4: 跑测试确认通过**

```bash
mvn -q -Dtest=ConceptNormalizerTest test
```

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add backend/src/main/java/com/motiflab/service/ConceptNormalizer.java backend/src/test/java/com/motiflab/service/ConceptNormalizerTest.java
git commit -m "feat: concept normalize and demo cache key"
```

---

### Task 3: DemoCache + 内置金牌「循环」HTML

**Files:**
- Create: `backend/src/main/java/com/motiflab/service/DemoCache.java`
- Create: `backend/src/test/java/com/motiflab/service/DemoCacheTest.java`
- Create: `backend/src/main/resources/demos/loop/L0.html`
- Create: `backend/src/main/resources/demos/loop/L1.html`
- Create: `backend/src/main/resources/demos/loop/L2.html`
- Create: `backend/src/main/resources/demos/loop/storyboard.json`

- [ ] **Step 1: 手写三份自包含 HTML（无外网）**

`L0.html` 要求：小人物重复「拿积木 → 叠高」3 次；每次循环计数 +1 可见；旁白不超过一句；纯 CSS/JS 动画。  
`L1.html`：同一故事，去掉旁白，放慢，只留计数。  
`L2.html`：只剩「重复三次叠积木」+ 超大数字。

每份文件头部注释：`<!-- motif-lab gold: loop Lx -->`。

- [ ] **Step 2: `storyboard.json`（循环本地骨架）**

```json
{
  "concept": "loop",
  "title": "循环",
  "beats": [
    {"who": "小明", "action": "拿起一块积木", "result": "手里有1块", "principle": "开始一次动作"},
    {"who": "小明", "action": "再拿一块叠上去", "result": "变成2块", "principle": "同样动作再做一次"},
    {"who": "小明", "action": "再叠一次", "result": "变成3块", "principle": "重复直到次数用完"},
    {"who": "旁白", "action": "停下来", "result": "塔高3", "principle": "循环=同一件事做多遍"}
  ]
}
```

- [ ] **Step 3: DemoCache 测试**

```java
@Test
void resolve_prefersClasspathGoldThenDisk() throws Exception {
    Path dir = Files.createTempDirectory("demo-cache");
    DemoCache cache = new DemoCache(dir, "v1");
    Optional<Path> hit = cache.resolve("loop", 0);
    assertTrue(hit.isPresent());
    assertTrue(Files.readString(hit.get()).contains("motif-lab gold"));
}
```

- [ ] **Step 4: 实现 `DemoCache`**

逻辑：`resolve(conceptId, level)` → 先查 `data/demo-cache/{cacheKey}.html`；没有则把 classpath `demos/{concept}/L{level}.html` 复制到缓存目录并返回；仍没有则 `Optional.empty()`。  
`put(conceptId, level, html)` 写入缓存文件。

- [ ] **Step 5: 跑测试**

```bash
mvn -q -Dtest=DemoCacheTest test
```

Expected: PASS。

- [ ] **Step 6: Commit**

```bash
git add backend/src/main/resources/demos backend/src/main/java/com/motiflab/service/DemoCache.java backend/src/test/java/com/motiflab/service/DemoCacheTest.java
git commit -m "feat: demo cache and gold loop animations L0-L2"
```

---

### Task 4: Storyboard 模型 + StoryboardService

**Files:**
- Create: `backend/src/main/java/com/motiflab/model/Storyboard.java`
- Create: `backend/src/main/java/com/motiflab/service/StoryboardService.java`
- Create: `backend/src/test/java/com/motiflab/service/StoryboardServiceTest.java`

- [ ] **Step 1: 模型**

```java
public record Storyboard(
    String conceptId,
    String title,
    List<Beat> beats
) {
    public record Beat(String who, String action, String result, String principle) {}
}
```

- [ ] **Step 2: 测试 — 循环走本地，禁止阻塞**

```java
@Test
void loop_usesClasspathJson_fast() {
    StoryboardService svc = new StoryboardService(new ConceptNormalizer(), null);
    long t0 = System.nanoTime();
    Storyboard sb = svc.getOrCreate("循环", 0);
    long ms = (System.nanoTime() - t0) / 1_000_000;
    assertEquals("loop", sb.conceptId());
    assertTrue(sb.beats().size() >= 3);
    assertTrue(ms < 500, "local storyboard must be fast, was " + ms);
}
```

- [ ] **Step 3: 实现**

`getOrCreate(concept, level)`：若 `normalize==loop`，读 `classpath:demos/loop/storyboard.json`；否则调用 `LlmClient.complete(shortPrompt)` 解析 JSON（v0.1 可先对非 loop 返回占位 beats + title=原概念，并标记 `experimental=true` 字段——若加字段需同步 record）。

为保持简单：**v0.1 非 loop 也先返回 3 条占位 beat**（标明试做），动画生成再调 LLM。骨架始终 <500ms。

- [ ] **Step 4: 跑测试 PASS 后 Commit**

```bash
git commit -m "feat: storyboard service with local loop seed"
```

---

### Task 5: MotifTutor 会话状态机（无 LLM）

**Files:**
- Create: `backend/src/main/java/com/motiflab/model/MotifSession.java`
- Create: `backend/src/main/java/com/motiflab/model/QuizItem.java`
- Create: `backend/src/main/java/com/motiflab/service/MotifTutorService.java`
- Create: `backend/src/test/java/com/motiflab/service/MotifTutorServiceTest.java`

- [ ] **Step 1: 会话模型**

```java
public class MotifSession {
    private String id;
    private String conceptRaw;
    private String conceptId;
    private int level;                 // 0..2
    private String sceneSeed;          // /重写 时变化
    private Storyboard storyboard;
    private String demoUrl;            // /api/demos/{sessionId}
    private String motto;              // 一句口诀
    private List<QuizItem> quiz;
    private String phase;              // STORYBOARD|DEMO|MOTTO_QUIZ|DONE
}
```

```java
public record QuizItem(String id, String question, List<String> choices, int answerIndex) {}
```

- [ ] **Step 2: 测试 start / simplify / rewrite**

```java
@Test
void start_loop_setsStoryboardAndCachedDemo() {
    // mock DemoCache resolve present, StoryboardService returns loop board
    MotifSession s = tutor.start(new StartLessonRequest("循环"));
    assertEquals(0, s.getLevel());
    assertEquals("STORYBOARD", s.getPhase()); // or DEMO if sync resolve
    assertNotNull(s.getStoryboard());
}

@Test
void simplify_bumpsLevelAndClearsDemoUntilResolved() {
    MotifSession s = tutor.start(...);
    tutor.simplify(s.getId());
    assertEquals(1, tutor.get(s.getId()).getLevel());
}

@Test
void simplify_atL2_staysL2() {
    // level 2 then simplify -> still 2, same cache key
}
```

- [ ] **Step 3: 实现内存 `ConcurrentHashMap` 会话库**

- `start`：normalize → storyboard → cache.resolve → 若命中则 `demoUrl` + phase=`MOTTO_QUIZ`（口诀/题可同步填金牌数据）；未命中 phase=`DEMO` 并异步生成（Task 6 接）。  
- `simplify`：level = clamp(level+1)；换 demo。  
- `rewrite`：`sceneSeed = UUID`；level 不变；使缓存键额外含 scene 哈希或强制未命中走生成（金牌重写也走生成）。  
  **v0.1 简化**：`/重写` 仅对非缓存路径；对 loop 金牌，rewrite 切换到「生成路径」并带 sceneSeed（Task 6）。若 Task 6 未就绪，rewrite 可暂时只换 storyboard 文案中的 who 名字。

- [ ] **Step 4: 金牌口诀与题（写死在服务内对 loop）**

口诀：`循环就是同一件事，按次数做多遍。`  
题 1：循环在做什么？选项：`只做一次` / `同样动作做多遍` / `随便做`；答案 index=1。  
题 2：上面叠积木叠了几次？`1` / `3` / `10`；答案 index=1。

- [ ] **Step 5: 测试 PASS + Commit**

```bash
git commit -m "feat: motif session start simplify rewrite and quiz for loop"
```

---

### Task 6: Lesson API + 演示文件 HTTP

**Files:**
- Create: `backend/src/main/java/com/motiflab/dto/*.java`
- Create: `backend/src/main/java/com/motiflab/controller/LessonController.java`
- Create: `backend/src/main/java/com/motiflab/controller/DemoController.java`
- Create: `backend/src/main/java/com/motiflab/dto/ApiResponse.java`

- [ ] **Step 1: API 契约**

| Method | Path | Body | 行为 |
|--------|------|------|------|
| POST | `/api/lessons` | `{ "concept": "循环" }` | start，返回 session JSON |
| POST | `/api/lessons/{id}/simplify` | `{}` | 升 L |
| POST | `/api/lessons/{id}/rewrite` | `{}` | 换场景 |
| POST | `/api/lessons/{id}/answer` | `{ "quizId","choiceIndex" }` | 判题 |
| GET | `/api/lessons/{id}` | | 取会话 |
| GET | `/api/demos/{sessionId}` | | `text/html` 返回当前动画；Header `Content-Security-Policy: sandbox` 由前端 iframe sandbox 为主 |

- [ ] **Step 2: 实现 Controller，统一 `ApiResponse<T>`**

- [ ] **Step 3: 用 curl 手动验金牌路径**

```bash
# 先 mvn spring-boot:run
curl -s -X POST http://127.0.0.1:30142/api/lessons -H "Content-Type: application/json" -d "{\"concept\":\"循环\"}"
curl -s http://127.0.0.1:30142/api/demos/<sessionId> | head
```

Expected: JSON 含 storyboard；HTML 含 `motif-lab gold`。

- [ ] **Step 4: Commit**

```bash
git commit -m "feat: lesson and demo HTTP APIs"
```

---

### Task 7: 前端两栏 UI + 闭环

**Files:**
- Create: `frontend/src/types.ts`
- Create: `frontend/src/api/client.ts`
- Create: `frontend/src/components/LessonLayout.tsx`
- Create: `frontend/src/components/ChatPanel.tsx`
- Create: `frontend/src/components/StagePanel.tsx`
- Create: `frontend/src/components/QuizCard.tsx`
- Modify: `frontend/src/App.tsx`

- [ ] **Step 1: types + api client**

```ts
export interface Beat { who: string; action: string; result: string; principle: string }
export interface Storyboard { conceptId: string; title: string; beats: Beat[] }
export interface QuizItem { id: string; question: string; choices: string[]; answerIndex?: number }
export interface MotifSession {
  id: string
  conceptRaw: string
  level: number
  storyboard: Storyboard
  demoUrl: string | null
  motto: string | null
  quiz: QuizItem[]
  phase: string
}
```

`api.startLesson(concept)` / `simplify` / `rewrite` / `answer` / `getLesson`。

- [ ] **Step 2: `StagePanel`**

- 若有 `storyboard`：先渲染 beats 列表（骨架）。  
- 若有 `demoUrl`：`<iframe sandbox="allow-scripts" src={demoUrl} title="demo" />`。  
- 生成中（phase=DEMO 且无 url）：显示「动画生成中…」。

- [ ] **Step 3: `ChatPanel`**

- 输入框默认「循环」快捷按钮。  
- 按钮：不懂/更简单 → `simplify`；更简单旁也可写「/简单点」。  
- 「换个故事」→ `rewrite`。  
- 显示 `motto`；`QuizCard` 答题。

- [ ] **Step 4: `LessonLayout` 左右分栏（大字号）**

- [ ] **Step 5: build + 浏览器手测**

```bash
cd frontend && npm run build
cd ../backend && mvn -q spring-boot:run
```

打开 `http://127.0.0.1:30142`：点循环 → 3 秒内见骨架 → iframe 播金牌 → 点更简单见 L1 → 答题通过。

- [ ] **Step 6: Commit**

```bash
git commit -m "feat: two-pane lesson UI with sandbox stage"
```

---

### Task 8: LLM 动画生成（非金牌路径）+ Provider 设置

**Files:**
- Create: `backend/src/main/java/com/motiflab/service/LlmClient.java`
- Create: `backend/src/main/java/com/motiflab/service/AnimationGenerator.java`
- Create: `backend/src/main/java/com/motiflab/service/ProviderService.java`
- Create: `backend/src/main/java/com/motiflab/service/CryptoService.java`
- Create: `backend/src/main/resources/prompts/animation-gen.md`
- Create: `backend/src/main/java/com/motiflab/controller/ProviderController.java`
- Create: `frontend/src/components/ProviderSettings.tsx`
- Modify: `MotifTutorService` 在 cache miss 时调用 generator

- [ ] **Step 1: `animation-gen.md` prompt**

要求模型只输出完整 HTML；必须体现 storyboard 每 beat；L1/L2 约束写入 user message；禁止外链。

- [ ] **Step 2: `AnimationGenerator.generate(storyboard, level, sceneSeed)`**

调 LLM → 校验含 `<html` → `demoCache.put` → 返回 path。失败抛明确错误。

- [ ] **Step 3: Provider 最小存取**

表 `llm_provider(id, api_key_enc, base_url, model, enabled)`；加密拷贝 Tepeu `CryptoService` 思路；`GET/PUT /api/provider`。

- [ ] **Step 4: cache miss 时异步生成**

`start`/`simplify`/`rewrite`：未命中则立即返回骨架，后台生成完成后会话 `demoUrl` 更新；前端每 1.5s `GET /api/lessons/{id}` 直到有 url 或 error 字段。

- [ ] **Step 5: 手测非 loop 概念（试做）或 loop+/重写**

Expected: 骨架先出；有 key 时最终出动画；无 key 时会话 `error` 可读。

- [ ] **Step 6: Commit**

```bash
git commit -m "feat: LLM animation generation and provider settings"
```

---

### Task 9: 验收对照 Spec + 文档收尾

**Files:**
- Modify: `CONTEXT.md`
- Modify: `README.md`

- [ ] **Step 1: 按 Spec §3.2 打勾**

| 标准 | 如何验 |
|------|--------|
| 3 秒内骨架 | 循环本地 storyboard；计时 |
| 缓存秒播 | 清网络仍能播 L0 |
| 更简单换档 | L0→L1 HTML 不同 |
| 检验题 | 答对进入 DONE |

- [ ] **Step 2: README 写清：端口 30142、如何配置 key、与 Tepeu/learn-skill 关系**

- [ ] **Step 3: CONTEXT 更新为「v0.1 闭环已通」或列出剩余缺口**

- [ ] **Step 4: Commit**

```bash
git commit -m "docs: v0.1 acceptance notes and README"
```

---

## Spec coverage checklist

| Spec 项 | Task |
|---------|------|
| 新项目薄壳 | 1 |
| 两栏 UI | 7 |
| 骨架秒出 | 3–4, 7 |
| 金牌循环 + 缓存 | 3, 5–6 |
| 不懂升 L / 更简单 | 2, 5–7 |
| /重写 | 5–8 |
| 口诀 + 界面题 | 5, 7 |
| sandbox iframe | 7 |
| LLM 现生成 | 8 |
| Provider | 8 |
| 不做记忆卡/shell/终端 | 全计划未列入 |

---

## Plan self-review

- 无 TBD/TODO 步骤；缓存键与 L0–L2 前后一致。  
- `/重写` 在金牌路径上的「强制生成」依赖 Task 8；Task 5 已写明过渡行为。  
- 单计划可交付可演示软件；数理/科学不在范围内。
