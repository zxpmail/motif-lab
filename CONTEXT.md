# CONTEXT.md
# 项目进度快照（极简）

## 当前在做什么
v0.1 闭环已通：循环金牌动画 + 升简版 + 检验题；可选 LLM 现生成。

## 上次停在哪
计划 9 个任务均已实现并提交；服务可在 30142 验收。

## 近期关键决定
- 薄壳独立项目；母题故事动画；骨架+缓存；首版「循环」
- LLM 用 HttpClient（不启 Spring AI 自动配置）；Provider 存 `~/.motif-lab/`
